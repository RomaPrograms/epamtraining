А в нашей лабе прям совсем не должно быть String-ов?

Как работает метод Connection.isValid(), в методе getConnection(), isValid()
вернёт на false если соединение было закрыто и с ним невозможно продолжать работу
и вот представим, что это действительно так и мы тогда за чем-то закрываем
соединение сново, в чём логика такого подхода?

И как соединение вообще может быть закрыто!?

Спросить по поводу локов, нужно ли мне создавать отдельный лок для каждого метода.

Обязательно добавить локи в мой connection pool.

У нас есть метод такой как chain.doFilter(). Как задать порядок фильтров в цепочке
фильтров.

Md5, BCrypt.

Кластерный и некластерный индексы?

    Помимо datetime есть есть ещё и timestamp, только timesamp хранит не дату, а колличество
секунд, и занимает он только 4 байта, так почему бы нам не хранить нашу дату в
таком типе?

    Что такое радужная хэш-функция?

    Сравнить структуру своего проекта с кем-нибудь ещё на всякий случай.
    Если нам требуется много где использовать join запросы, то зачем тогда
впринципе нужен сервис?

    Что касается того, что нельзя, чтобы было более одной точки в строке, тупо
перенос строки на одну вниз решает нашу проблему или нет?

    Там, где review нужно исправить пару фич.

    Поменять тот момент, что в сессии есть атрибут с именем action. И добавить оработчик ошибок.

    Спросить, зачем им в action-е там где форвард нужна мэпа.

    Совсем ли это плохо, сессия при редиректе.