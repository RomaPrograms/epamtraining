А в нашей лабе прям совсем не должно быть String-ов?

Как работает метод Connection.isValid(), в методе getConnection(), isValid()
вернёт на false если соединение было закрыто и с ним невозможно продолжать работу
и вот представим, что это действительно так и мы тогда за чем-то закрываем
соединение сново, в чём логика такого подхода?

Md5, BCrypt.

    Помимо datetime есть есть ещё и timestamp, только timesamp хранит не дату, а колличество
секунд, и занимает он только 4 байта, так почему бы нам не хранить нашу дату в
таком типе?

    Что такое радужная хэш-функция?

    Сравнить структуру своего проекта с кем-нибудь ещё на всякий случай.

    Добавить кнопку забыли пароль если будет время.

    там где у меня ревью решить проблему с передачей в сессии дома.

    Можно ли оставить дохера скрытых кнопок как я и сделал?

    Сделать норм валидацию для дома

    Разобраться с грантами

    Можно ли вместо одной формы делать кучу форм и кучу невидимых input-ов

    убрать css штуки

    Убрать reflection api

    добавить вместо error page норм страничку

    Разобраться с шифровкой пароля убрать его из профиля в сессии

    разобраться с фотками

    Когда логинимся на странице регистрации, то всё ломается.

    Разобраться, где хранить все эти файлы касательно BCrypt.

    Добавить валидацию без бутстрапа.