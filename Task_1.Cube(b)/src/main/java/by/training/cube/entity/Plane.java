/*
 * These package contain enum {@code Plane}.
 *
 * @since 1.0
 * @author Roman
 * @version 1.0
 */
package by.training.cube.entity;

/**
 * Plane contain different kinds of planes.
 */
public enum Plane {
    /**
     * plane which created from coordinates X and Y.
     */
    XY,
    /**
     * plane which created from coordinates X and Z.
     */
    XZ,
    /**
     * plane which created from coordinates Y and Z.
     */
    YZ
}
