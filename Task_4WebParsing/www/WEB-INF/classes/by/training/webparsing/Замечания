В комментариях там, где getter-ы прописываем "Gets the value of the dateOfDelivery property."
В комметариях там, где setter-ы прописываем "Sets the value of ..."
Нельзя забывать прописывать автора в комментариях

    Демаршаллизация - процесс преобразования XML-данных в java класс.
    Маршализация - процесс преобразования java-данных в XML-документ.
    Семантическая эквивалентность: Равенство, основанное на XML-правилах. Два
документа могут быть семантически эквивалентны, даже если они не выглядят
одинаково - смотрите примеры в предыдущей статье.
    Кругооборот данных: Полный цикл прохождения данных от XML-документа через
Java-код и обратно в XML. Эффективный кругооборот гарантирует идентичность
(семантическую эквивалентность) входных и выходных документов.
    Демаршаллизацию можно проводить двумя способами это из программы и из
командной строки при помощи xjc.
    В нашем проекте должен быть только один сервлет, не более.

Нужно доделать логгеры

В методе main() нужно разделить все строки на отдельные функции.

что такое INT(11)

использовать не index.jsp, а использовать другую jsp